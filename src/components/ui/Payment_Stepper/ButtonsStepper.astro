---
interface Props {
	title: string;
	description: string;
	listButtons: Array<{
		id: string;
		IdButton: string;
		Div1Id: string;
		Div2Id: string;
		Span: string;
		title: string;
		description: string;
		icon: string;
	}>;
}

const { title, description, listButtons } = Astro.props;
---

<div class="ml-5 hidden w-full lg:flex lg:min-h-175 lg:flex-col">
	<div class="mt-16">
		<h3 class="text-black-900 text-2xl dark:text-white">
			{title}
		</h3>
		<p class="w-3/4 text-gray-500">
			{description}
		</p>
	</div>
	<ol class="mt-8 w-full space-y-8 overflow-hidden">
		{
			listButtons.map((i, index) => (
				<li
					id={i.id}
					class="relative flex-1 after:absolute after:-bottom-11 after:left-1/4 after:inline-block after:h-full after:w-0.5 after:content-['']"
				>
					<button
						class="flex w-full max-w-sm items-center justify-center gap-8"
						id={i.IdButton}
					>
						<div
							id={i.Div1Id}
							class="relative z-10 flex w-full items-center gap-3.5 rounded-xl p-3.5 text-left"
						>
							<div
								id={i.Div2Id}
								class="flex items-center justify-center rounded-lg"
							>
								<span id={i.Span} class="p-3 text-white">
									<Fragment set:html={i.icon} />
								</span>
							</div>
							<div class="flex flex-col items-start justify-center rounded-md">
								<h6 id={`steph6-${index + 1}`} class="mb-0.5 text-base font-semibold">
									{i.title}
								</h6>
								<p id={`stepp-${index + 1}`} class="text-xs font-normal">
									{i.description}
								</p>
							</div>
						</div>
					</button>
				</li>
			))
		}
	</ol>
</div>
