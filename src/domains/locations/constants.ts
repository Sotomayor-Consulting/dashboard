/**
 * ID del país Estados Unidos en la tabla `countries`. Verificado vía MCP:
 *   SELECT id FROM countries WHERE iso = 'US' → 75
 *
 * Se usa como default para la jurisdicción de incorporación (LLC solo en US).
 */
export const US_COUNTRY_ID = 75;
