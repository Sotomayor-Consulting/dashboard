---
import { Icon } from 'astro-icon/components';
interface Props {
	tasks: {
		title: string;
		description: string;
		date?: string;
		completed: string;
		icon: string;
		action?: {
			label: string;
			href: string;
			icon?: string;
		};
	}[];
	completedCount: number;
	totalCount: number;
	progressPercent: number;
}
const { tasks, completedCount, totalCount, progressPercent } = Astro.props;
---

<div
	class="rounded-lg border border-gray-200 bg-white p-4 shadow-md shadow-gray-200/70 sm:p-6 2xl:col-span-2 dark:border-gray-700 dark:bg-transparent dark:shadow-none"
>
	<div
		class="mb-5 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"
	>
		<div>
			<h2 class="text-xl font-semibold text-gray-900 dark:text-white">
				Proceso de incorporación
			</h2>
			<p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
				Sigue el avance etapa por etapa de tu empresa.
			</p>
		</div>
		<div class="flex items-center gap-2">
			<span
				class="dark:bg-primary-gold rounded-md bg-gray-100 px-2 py-1 text-xs font-medium text-gray-700 dark:text-white"
			>
				{completedCount}/{totalCount} completadas
			</span>
			<span
				class="dark:bg-primary-gold/20 rounded-md bg-[#e8efff] px-2 py-1 text-xs font-semibold text-[#134aed] dark:text-amber-100"
			>
				{progressPercent}%
			</span>
		</div>
	</div>

	<div class="px-1 pb-1">
		<ol class="relative border-s border-gray-200 pl-1 dark:border-gray-700">
			{
				tasks.map((li) => (
					<li class="ms-6 mb-7 flex items-start justify-between gap-3 last:mb-0">
						<div class="min-w-0">
							<span
								class:list={{
									'dark:ring-black-900 absolute -inset-s-4 flex h-8 w-8 items-center justify-center rounded-full ring-4 ring-white': true,
									'bg-success-soft text-fg-success-strong':
										li.completed === 'Completado',
									'bg-neutral-tertiary text-body':
										li.completed === 'Por completar',
									'bg-primary-gold animate-pulse text-white':
										li.completed === 'En proceso',
								}}
							>
								<Icon
									name={li.icon}
									class:list={{
										'text-base': true,
										'animate-pulse': li.completed === 'En proceso',
									}}
								/>
							</span>

							<h3
								class:list={{
									'leading-tight font-semibold': true,
									'text-fg-success-strong': li.completed === 'Completado',
									'text-gray-800 dark:text-gray-200':
										li.completed === 'Por completar',
									'text-primary-gold': li.completed === 'En proceso',
								}}
							>
								{li.title}
							</h3>
							<p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
								{li.description}
							</p>
							<div class="mt-2 flex flex-wrap items-center gap-2">
								<span
									class:list={{
										'inline-flex rounded-md px-2 py-0.5 text-[11px] font-medium': true,
										'bg-emerald-50 text-emerald-700 dark:bg-emerald-900/25 dark:text-emerald-300':
											li.completed === 'Completado',
										'text-primary-gold dark:bg-primary-gold/25 bg-blue-50 dark:text-amber-100':
											li.completed === 'En proceso',
										'dark:bg-primary-gold bg-gray-100 text-gray-600 dark:text-gray-300':
											li.completed === 'Por completar',
									}}
								>
									{li.completed}
								</span>
								{li.action && (
									<a
										href={li.action.href}
										class="bg-primary-gold hover:bg-primary-gold/50 inline-flex items-center gap-1.5 rounded-md px-2.5 py-1 text-[11px] font-semibold text-white transition"
									>
										{li.action.icon && (
											<Icon name={li.action.icon} class="h-3.5 w-3.5" />
										)}
										{li.action.label}
									</a>
								)}
							</div>
						</div>
						<div class="mt-0.5 hidden rounded-md bg-gray-50 px-2 py-1 text-xs text-gray-500 lg:block dark:bg-black dark:text-gray-400">
							{li.date}
						</div>
					</li>
				))
			}
		</ol>
	</div>
</div>
