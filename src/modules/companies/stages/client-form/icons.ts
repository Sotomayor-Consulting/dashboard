// Re-export del registro shared de Remix Icons para `@iconify/react`.
// Mantenemos este archivo por compatibilidad con los imports existentes
// (`import '../icons'`), pero la implementación vive en `@shared/iconify-ri`.
import '@shared/iconify-ri';
