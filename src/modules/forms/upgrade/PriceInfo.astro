---
import Stripelogo from '@assets/stripe.svg';

const { UPGRADE_BASE_PRICE, processingFee, totalAmount } = Astro.props;
---

<div class="mt-10 mb-16 w-full grow md:w-3/5">
	<div
		id="price-summary"
		class="dark:bg-black-900 space-y-4 rounded-lg border border-gray-100 bg-gray-50 p-6 dark:border-gray-700"
	>
		<div class="space-y-2">
			<dl class="flex items-center justify-between gap-4">
				<dt class="text-base font-normal text-gray-500 dark:text-gray-400">
					Precio del upgrade
				</dt>
				<dd
					id="summary-base"
					class="text-base font-medium text-gray-900 dark:text-white"
				>
					${UPGRADE_BASE_PRICE.toFixed(2)}
				</dd>
			</dl>

			<dl class="flex items-center justify-between gap-4">
				<dt class="text-base font-normal text-gray-500 dark:text-gray-400">
					Tarifas de procesamiento
				</dt>
				<dd
					id="summary-fee"
					class="text-base font-medium text-gray-900 dark:text-white"
				>
					${processingFee.toFixed(2)}
				</dd>
			</dl>
		</div>

		<dl
			class="flex items-center justify-between gap-4 border-t border-gray-200 pt-2 dark:border-gray-700"
		>
			<dt class="text-base font-bold text-gray-900 dark:text-white">Total</dt>
			<dd
				id="summary-total"
				class="text-base font-bold text-gray-900 dark:text-white"
			>
				${totalAmount.toFixed(2)}
			</dd>
		</dl>

		<p class="text-sm text-gray-500 dark:text-gray-400" id="summary-note">
			El upgrade se cobra con un valor fijo de $600.00 mas la comision de
			procesamiento.
		</p>
	</div>

	<div class="mt-6 flex items-center justify-center gap-2">
		<p class="text-base font-normal text-gray-500 dark:text-gray-400">
			Pago seguro gestionado por:
		</p>
		<Stripelogo width={80} />
	</div>
</div>
