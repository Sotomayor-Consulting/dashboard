---
// La tabla de incorporaciones se migró a /admin/incorporaciones (rediseño).
// Mantenemos /incorporations/[id] como vista de detalle del proceso.
return Astro.redirect('/admin/incorporaciones', 301);
---
